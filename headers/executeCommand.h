#ifndef executeCommandHeader
#define executeCommandHeader

#include <stdlib.h>
#include <stdbool.h>
#include <string.h>

bool executeCommand(size_t argumentCount, char ***arguments);

#endif // executeCommandHeader
